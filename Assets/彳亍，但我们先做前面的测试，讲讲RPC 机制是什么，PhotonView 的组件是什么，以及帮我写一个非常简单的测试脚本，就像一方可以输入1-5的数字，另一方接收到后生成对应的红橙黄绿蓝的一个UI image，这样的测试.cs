using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class 彳亍，但我们先做前面的测试，讲讲RPC机制是什么，PhotonView的组件是什么，以及帮我写一个非常简单的测试脚本，就像一方可以输入1-5的数字，另一方接收到后生成对应的红橙黄绿蓝的一个UIimage，这样的测试 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
